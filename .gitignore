# Local graph viz (not part of project)
ayiru_graph_3d.html
tools/scripts/graph_view_3d.py

# Agent handoff / operating notes — local only, not tracked
AGENTS.md
HANDOFF.md

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*.swo
*.tmp
*.bak
*~
.idea/
.vscode/
*.code-workspace

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
.venv/
*.egg-info/
build/
dist/
.tox/

# Databases (local SQLite files)
ayiru.db
*.db
*.db-journal
*.db-wal
*.db-shm

# Per-run results / spikes / scratch
phase0_results.json
/tmp/

# Personal Claude Desktop regression harness — local-only, never committed
claude_desktop_regression/

# Defense-in-depth: ANY .env file anywhere in the tree is gitignored.
# .env.example files stay tracked (templates, no secrets).
.env
**/.env
!.env.example
!**/.env.example
